

// copy this to private_credentials.h
// set ssid/wpakey to those used on the Raspi 3
// the Pi creates its own access point for the reader
// this is NOT your local LAN access point

// also set your .gitconfig to not check in private_* files

char* ssid = "pi_ap_ssid";
char* wpakey = "pi_ap_passwd";

